namespace AGC.Launcher.ViewModels;

/// <summary>Shown briefly on launch while the real startup work (session check, maintenance
/// status) runs, so the app doesn't jump straight from a blank window to content.</summary>
public sealed class SplashViewModel : ViewModelBase
{
}
